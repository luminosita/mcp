# Application Configuration
APP_NAME=AI Agent MCP Server
APP_VERSION=0.1.0
APP_PATH_NAME=mcp_server
DEBUG=false

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Database Configuration
# PostgreSQL + pgvector connection string for local development
# Format: postgresql+asyncpg://user:password@host:port/database
# Note: asyncpg driver required for async SQLAlchemy operations
DATABASE_URL=postgresql+asyncpg://mcp_user:dev_password_change_in_production@localhost:5432/mcp_dev

# Logging Configuration
# LOG_LEVEL options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
# LOG_FORMAT options: json (production), text (development)
LOG_FORMAT=json
